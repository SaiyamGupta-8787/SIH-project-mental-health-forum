# ============================
# Example Environment Variables
# Copy this file as ".env"
# and replace the placeholder values
# ============================

# Database credentials
DB_USER=root               # Your MySQL username
DB_PASSWORD=yourpassword   # Your MySQL password
DB_HOST=localhost          # Usually localhost (or 127.0.0.1)
DB_NAME=mental_health_db   # Database name (create it in MySQL before running)

# Flask secret key
SECRET_KEY=replace_with_random_string   # Any random string (used for sessions)
